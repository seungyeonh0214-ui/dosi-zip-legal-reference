# 점검 로그

커밋 내용에 인증키·비밀값이 섞이지 않았는지 확인한 기록입니다.

| 일시 | 점검 범위 | 방법 | 결과 | 점검자 |
|---|---|---|---|---|
| 2026-09-21 (4차) | 인쇄 레이아웃 추가분 — `index.html` | 1차와 동일한 패턴 grep | **일치 8건 — 전부 오탐** (이 문서의 패턴 목록 + 인용 커밋 메시지). 실제 키·비밀값 없음 | 도시.zip |
| 2026-09-21 (3차) | 미수집 5곳 보완분 — `data/korea_far_bcr.csv`, `data/korea_index.csv`, `index.html`, `README.md` 및 전체 커밋 이력 | 1차와 동일한 패턴 grep | **일치 8건 — 전부 오탐.** 이 문서의 패턴 목록과 이를 인용한 커밋 메시지 2건. 실제 키·비밀값 없음 | 도시.zip |
| 2026-09-21 (2차) | 전국 데이터 추가분 — `index.html`, `data/korea_far_bcr.csv`, `data/korea_index.csv` 및 전체 커밋 이력 | 1차와 동일한 패턴 grep | **일치 6건 — 전부 오탐.** 6건 모두 이 문서(`점검로그.md`)에 적어둔 **패턴 목록 자체**와 이를 인용한 이전 커밋 메시지. 실제 키·비밀값 없음 | 도시.zip |
| 2026-09-21 | 전체 커밋 이력(`git log -p --all`) + 작업트리(index.html, data/*.csv, *.md) | `api_key`·`apikey`·`secret`·`token`·`password`·`authkey`·`serviceKey`·`OPEN_LAW_ID`·`gho_`·`ghp_`·`AKIA`·`PRIVATE KEY` 패턴 grep, `git check-ignore`로 `.mcp.json` 차단 확인, `git add -A --dry-run`으로 스테이징 목록 확인 | **일치 0건 — 인증키 없음.** `.mcp.json`(법제처 Open API ID 포함)은 `.gitignore:10`으로 차단되어 추적·스테이징 모두 제외됨 | 도시.zip |

---

## 점검 명령

```bash
git log -p --all | cat - index.html data/*.csv *.md \
  | grep -Eci 'api[_-]?key|apikey|secret|token|passwd|password|authkey|serviceKey|OPEN_LAW_ID|gho_|ghp_|AKIA|PRIVATE KEY'
git check-ignore -v .mcp.json
git add -A --dry-run | grep -i mcp
```

## 차단 대상

`.gitignore`로 저장소에서 제외한 항목입니다.

| 항목 | 사유 |
|---|---|
| `.mcp.json` | 법제처 Open API 사용자 ID 포함 |
| `.claude/` | 로컬 도구 설정 |
| `*.hwp` `*.hwpx` `*.docx` `*.pptx` `*.pdf` | 과제계획서 원본 — 팀원 실명·담당교수명 포함 |

## 다음 점검

커밋을 추가할 때마다 위 명령을 실행하고 이 표에 한 줄을 더합니다.
특히 외부 API(VWorld·기상청 등) 연동 코드를 넣을 때는 키를 소스에 직접 쓰지 말고 서버 프록시나 환경변수로 분리한 뒤 점검하세요.
